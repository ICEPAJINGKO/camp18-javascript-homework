function squareArr(arr){
    return arr.map(function(x){
        return x*x;
    })
}

/*
Lab2 Array: การคืนค่า Array
ให้เขียนฟังก์ชัน squareArr(arr) เพื่อคืนค่า Array ที่มี element เป็นเลขยกกำลังสองของแต่ละ element ใน arr

ตัวอย่างผลลัพธ์
*/